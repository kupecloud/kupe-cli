name: Publish

on:
  # Normal path: called by main.yaml after semantic-release tags a release.
  workflow_call:
    inputs:
      version:
        description: Semantic-release version without the leading v (e.g. "0.1.0").
        required: true
        type: string
    secrets:
      HOMEBREW_TAP_TOKEN:
        required: true
      SCOOP_BUCKET_TOKEN:
        required: true
  # Hotfix path: a human cuts a tag manually (e.g. when semantic-release is
  # unavailable, or a release needs to be re-attempted). Run this from the
  # Actions tab on the desired branch with the version of the existing tag
  # — the job checks out `v<version>` and runs goreleaser the same way.
  workflow_dispatch:
    inputs:
      version:
        description: Existing tag's version without the leading v (e.g. "0.1.0").
        required: true
        type: string

permissions:
  contents: write
  packages: write
  id-token: write

jobs:
  publish:
    name: Attach release artifacts
    runs-on: ubuntu-latest
    env:
      GORELEASER_VERSION: v2.15.2
    steps:
      - name: Checkout the release tag
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          ref: v${{ inputs.version }}
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@4a3601121dd01d1626a1e23e37211e3254c1c06c # v6
        with:
          go-version-file: go.mod
          cache: true

      - name: Install Cosign
        uses: sigstore/cosign-installer@3454372f43399081ed03b604cb2d021dabca52bb # v3

      - name: Install Syft
        uses: anchore/sbom-action/download-syft@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0

      - name: Install GoReleaser
        shell: bash
        run: |
          GOBIN="$RUNNER_TEMP/bin" go install "github.com/goreleaser/goreleaser/v2@${GORELEASER_VERSION}"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"

      - name: Run GoReleaser
        # release.mode: append in .goreleaser.yaml tells goreleaser to upload
        # artifacts into the GitHub release that semantic-release already
        # created, rather than creating a new (conflicting) release.
        run: goreleaser release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          SCOOP_BUCKET_TOKEN: ${{ secrets.SCOOP_BUCKET_TOKEN }}
